function getWordBreakUtility(utilityClass) {
	const wordBreakClasses = ["break-normal", "break-words", "break-all"];

	if (wordBreakClasses.includes(utilityClass)) {
	}
}

module.exports = getWordBreakUtility;
