function getWhiteSpaceUtility(utilityClass) {
	const whiteSpaceClasses = [
		"whitepspace-normal",
		"whitepspace-nowrap",
		"whitepspace-pre",
		"whitepspace-pre-line",
		"whitepspace-pre-wrap",
	];

	if (whiteSpaceClasses.includes(utilityClass)) {
	}
}

module.exports = getWhiteSpaceUtility;
