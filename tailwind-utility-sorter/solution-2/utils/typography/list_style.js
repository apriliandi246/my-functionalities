function getListUtility(utilityClass) {
	const listClasses = [
		"list-none",
		"list-disc",
		"list-decimal",
		"list-inside",
		"list-outside",
	];

	if (listClasses.includes(utilityClass)) {
	}
}

module.exports = getListUtility;
