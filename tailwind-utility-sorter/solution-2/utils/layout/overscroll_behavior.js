function getOverscrollBehaviorUtility(utilityClass) {
	const overscrollBehaviors = [
		"overscroll-auto",
		"overscroll-contain",
		"overscroll-none",
		"overscroll-y-auto",
		"overscroll-y-contain",
		"overscroll-y-none",
		"overscroll-x-auto",
		"overscroll-x-contain",
		"overscroll-x-none",
	];

	if (overscrollBehaviors.includes(utilityClass)) {
	}
}

module.exports = getOverscrollBehaviorUtility;
