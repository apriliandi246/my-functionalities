function getClearUtility(utilityClass) {
	const clearClasses = [
		"clear-left",
		"clear-rigth",
		"clear-both",
		"clear-none",
	];

	if (clearClasses.includes(utilityClass)) {
	}
}

module.exports = getClearUtility;
